<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="SwipeItemLayout">
        <attr name="swipeMode" format="integer">
            <enum name="DRAG_LEFT" value="0" />
            <enum name="DRAG_RIGHT" value="1" />
            <enum name="DRAG_BOTH" value="2" />
        </attr>
        <attr name="dragContentId" format="reference" />
        <attr name="dragItemTailId" format="reference" />
        <attr name="dragItemHeadId" format="reference" />
    </declare-styleable>

</resources>
